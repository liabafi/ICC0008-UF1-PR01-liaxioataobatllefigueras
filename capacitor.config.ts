import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ICC0008-UF1-PR01-liaxioataobatllefigueras',
  webDir: 'www'
};

export default config;
